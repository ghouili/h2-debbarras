# WORKLOG — Services Polish Pass

Checklist:
- app/services/page.tsx: pluralization for “Inclut”, detail link per hero card, single-card grid balancing, bottom CTA copy, tabs ARIA already handled.
- components/ui/tabs.tsx: reviewed (Radix-based, not used on /services after custom tabs).
- lib/config.ts: service slugs confirmed for detail links.
- lib/campaign-map.ts: campaign map unchanged (links remain consistent with slugs).
